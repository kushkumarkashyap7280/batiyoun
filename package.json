{
  "name": "batiyoun-root",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "common"
  ],
  "scripts": {
    "dev": "echo \"Run client and server separately\"",
    "setup": "npm install && npm run build -w @batiyoun/common"
  },
  "devDependencies": {
    "@types/node": "^25.2.0",
    "tsx": "^4.21.0"
  }
}
